# Build-time dependency, pinned above PYSEC-2025-49 and PYSEC-2026-3447
setuptools>=83.0.0

# Core data / modelling
numpy>=1.26,<3
pandas>=2.2
scikit-learn>=1.5
xgboost>=2.1
shap>=0.46
joblib>=1.4
pyarrow>=17

# Live mode: windowed reads of USGS's aquatic-reflectance COGs + tile lookup
rasterio>=1.4
pyproj>=3.6
mgrs>=1.4

# API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
httpx>=0.27
groq>=0.11
python-dotenv>=1.0

# Required transitively: `import shap` pulls in shap.plots, which imports matplotlib at module level
matplotlib>=3.9

# Test runner. Kept here rather than in a separate dev file: it is one small package, and a second
# requirements file costs more in confusion than it saves in image size.
pytest>=8

# Not installed here — the benchmark scripts only, and a large download:
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
